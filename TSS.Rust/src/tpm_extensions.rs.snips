/*
    This file contains source-code snippets that the code-generator inserts into the 
    appropriate Rust type implementation file. Note - only fields are added to the 
    relevant structs. Extension methods implementations may be found in tpm_type_extensions.rs
*/

>> TPM_HANDLE

/// The authorization value associated with this handle
pub auth_value: Vec<u8>,

/// The name associated with this handle
pub name: Vec<u8>,
